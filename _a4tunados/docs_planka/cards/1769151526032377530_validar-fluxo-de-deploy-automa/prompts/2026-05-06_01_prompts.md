---
title: "Prompts — Card 377530 Validar deploy staging"
tags:
  - a4tunados
  - tuninho/escriba
  - type/prompt
  - status/active
date: 2026-05-06
version: "1.0"
related:
  - "[[../sessoes/2026-05-06_01_card-377530-validar-deploy]]"
---

# Prompts — Card 377530

Registro literal dos prompts do operador @victorgaudio nesta sessao card-isolated.

---

## Prompt 1 — Pergunta inicial sobre senha

> @[tuninho](1770000000000000001) , qual e a senha do admin para acessar a plataforma?

**Contexto**: primeira interacao no card. Acabou de receber o card-isolated.
**Resultado**: investigacao mostrou que `.env` real esta em
`hostinger-beta:/opt/hostinger-beta/planka/.env` (deliberadamente fora do repo).
Puxei via SSH e respondi no card mural: `victorgaudio@4tuna.com.br` /
`fqOAGypE1JB-C2Ji4LhxEBm1` / username `admin`.

---

## Prompt 2 — Pedido de investigacao dos forms

> os formularios nao estao funcionando corretamente, provavelmente pq precisam dos
> respectivos quadros e colunas criados para alimentarem cards neles. consegue
> investigar essa situacao dos dois forms para que possamos resolver eles no deploy?

**Contexto**: operador detectou que os 2 forms (`form-pdviewerp-stagging.fourtuna.com.br`)
nao funcionam. Hipotese inicial: faltam boards/colunas.

**Resultado**: investigacao end-to-end (codigo do ticket-form, .env, estado do Planka,
reproducao via curl + dentro do container ticket-form). Hipotese inicial **descartada**:
boards e listas existem e IDs estao corretos no `.env`. **2 root causes diferentes**
identificadas:
1. Planka rejeita `description: ""` com `E_MISSING_OR_INVALID_PARAMS` (atinge ambos
   os forms).
2. `PRIORITY_LABELS` no `.env` esta com IDs vazios apos o `:` porque o board "Chamados"
   nao tem labels criadas (atinge so o Chamado Tecnico).

Diagnostico postado no card mural com plano de fix de 6 passos.

---

## Prompt 3 — Autorizacao de execucao completa

> @[tuninho](1770000000000000001) preciso que vc crie tudo que precisa ser criado
> para rodar e faca o deploy novamente pela branch deploy/staging e confira se esta
> funcionando o fluxo de preenchimento de formulario pelo link do deploy

**Contexto**: operador autoriza execucao do plano sem necessidade de confirmacao
adicional intermediaria. Modo pragmatico autonomo continua.

**Resultado**: 6 passos executados em sequencia:
1. ✅ Criadas 4 colunas extras (Em Execucao + Concluido/Executados nos 2 boards)
2. ✅ Criadas 8 labels de prioridade no board Chamados
3. ✅ Atualizado `PRIORITY_LABELS` no `.env` server (com backup)
4. ✅ Fix `ticket-form/src/planka.js` (commit `121c571d`)
5. ✅ Fix adicional necessario em `scripts/deploy.sh` (sem `--build` o ticket-form
   nao rebuilda — descoberto durante deploy)
6. ✅ Deploy via SSH+rsync+deploy.sh (workflow Actions ainda nao instalado)
7. ✅ Smoke API + Smoke E2E via Playwright nos 2 forms

Todos os smokes passaram. Reportei pendencia: workflow GitHub Actions ainda nao
estava ativo (token gh sem scope `workflow`).

---

## Prompt 4 — Pergunta sobre como ativar workflow

> como faco isso de ativar o workflow? qual o passo a passo?

**Contexto**: operador pediu instrucao pra resolver a pendencia.

**Resultado**: postei no card mural 2 caminhos: (A) terminal com
`gh auth refresh -h github.com -s workflow` + cherry-pick do template, ou (B) UI do
GitHub criando arquivo `.github/workflows/deploy-staging.yml` direto na branch
`deploy/staging` com YAML completo do template colado.

---

## Prompt 5 — Manter conversa no terminal + tentar executar

> nao precisa por enquanto mandar mais msgs no card. vamos por aqui. vc consegue
> executar esses comandos por mim? assim consegue acompanhar se rolou ou nao

**Contexto**: operador quer que eu execute, mas o `gh auth refresh` e interativo
(precisa de browser).

**Resultado**: tentei via `gh api PUT contents/.github/workflows/deploy-staging.yml`
direto — bloqueado tambem (404, mesmo motivo de scope). Pedi pro operador rodar 1
comando interativo (`! gh auth refresh -h github.com -s workflow`) ou usar UI.

---

## Prompt 6 — Workflow ativado via UI

> fiz pela UI e commitei. consegue testar pra ver se esta ok?

**Contexto**: operador commitou `.github/workflows/deploy-staging.yml` direto pelo
GitHub UI na branch `deploy/staging`.

**Resultado**: verifiquei que:
- Commit `310d47e2 Add GitHub Actions workflow for staging deployment` chegou
- Workflow run `25457526463` disparou automaticamente
- 9/9 steps completed/success em ~1m02s
- Smoke API pos-deploy continuou OK nos 2 forms
- Reportei sucesso no terminal

---

## Prompt 7 — Trigger desta documentacao

> ok. chama o escriba e devolve pf pra develop via gitflow da for

**Contexto**: encerramento — operador pede documentacao via Escriba e merge para
develop via skill `tuninho-git-flow-dafor`.

**Resultado**: esta documentacao + git-flow d'a4 a seguir.
