docker-compose.override.yml
.idea
.DS_Store

# Production secrets — NEVER commit
.env
.env.local
.env.*.local

node_modules

# Prevent another lockfile than package-lock.json (npm) from being created
# If some case you are using pnpm or yarn, don't forget to generate npm lockfile
# before commiting your code by running:
# `npm i --package-lock-only`
pnpm-lock.yaml
yarn.lock

# Chart dependencies
**/charts/*.tgz

# a4tunados-ops-suite cache
_a4tunados/.cache/

# Tuninho hook/session state (local only)
.claude/session-tracker.json
.claude/settings.local.json
.claude/tuninho-*-state.json
.claude/tuninho-briefing-pending.json

# Worktree symlinks (apontam pra workspace dev humano)
/.mcp.json
/_a4tunados/env

# Playwright MCP test artifacts (cache local, nao versionar)
.playwright-mcp/
